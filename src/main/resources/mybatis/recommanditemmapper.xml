<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chflower.mapper.RecommandItemMapper">

    <select id="selectall" resultType="RecommandItem">
        select *
        from  (select * from item order by dbms_random.value)
        where <![CDATA[ rownum < 5 ]]>
    </select>

    <select id="select100" resultType="RecommandItem">
        SELECT *
        FROM (SELECT * FROM item WHERE category_id IN (100, 200) ORDER BY dbms_random.value)
        where <![CDATA[ rownum < 4 ]]>
    </select>

    <select id="select300" resultType="RecommandItem">
        SELECT *
        FROM (SELECT * FROM item WHERE category_id IN (300, 400) ORDER BY dbms_random.value)
        where <![CDATA[ rownum < 4 ]]>
    </select>

    <select id="selectsubs" resultType="RecommandItem">
        SELECT *
        FROM (SELECT * FROM subsitem ORDER BY dbms_random.value)
        where <![CDATA[ rownum < 4 ]]>

    </select>
</mapper>